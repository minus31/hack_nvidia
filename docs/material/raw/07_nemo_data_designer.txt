NeMo Data Designer Quick Start Guide
NeMo Data Designer — 스키마 기반으로 합성 데이터셋을 생성하는 NVIDIA의 프레임워크. LLM을 활용한 고품질 학습 데이터 생성 파이프라인을 YAML 스키마로 선언적으로 구성합니다.
Docs: nvidia-nemo.github.io/DataDesigner/latest/  |  GitHub: github.com/NVIDIA-NeMo/DataDesigner
Nemotron 3 & vLLM을 활용한 한국어 수학 CoT 데이터셋 생성
Nemotron 해커톤 Track C — 합성 데이터 생성 파이프라인
이 튜토리얼에서는 vLLM으로 로컬 배포한 NVIDIA Nemotron 3를 활용하여, 한국 초·중등 수학 문제와 단계별 Chain-of-Thought(CoT) 풀이를 생성하는 합성 데이터 파이프라인을 구축합니다.
모델	파라미터	활성 파라미터	적합한 용도
Nemotron 3 Super	120B	12B	고품질 데이터 생성, 프로덕션 데이터셋
Nemotron 3 Nano	30B	3B	리소스 효율적, 빠른 프로토타이핑, 단일 GPU 환경
왜 합성 수학 데이터인가?
희소성: 고품질 한국어 수학 추론 데이터셋은 영어에 비해 매우 부족
비용: 수동으로 CoT 어노테이션을 만드는 것은 비용과 시간이 많이 소요
제어: 합성 파이프라인으로 난이도, 주제 분포, 추론 깊이를 제어 가능
확장성: 몇 분 만에 수천 개의 예제를 생성 가능
아키텍처
Nemotron 3 Super/Nano (vLLM 서버) <-- OpenAI 호환 API --> NeMo Data Designer --> 한국어 수학 CoT 데이터셋

파이프라인 개요:

vLLM 배포 — Nemotron 3 Super 또는 Nano를 OpenAI 호환 API로 서빙
샘플러 컬럼 — 수학 주제, 난이도, 학년 분포 제어
LLM 텍스트 컬럼 — 한국어 문제와 단계별 풀이 생성
LLM 구조화 컬럼 — 구조화된 메타데이터 추출 (정답, 사용 개념)
LLM 심사 컬럼 — 생성된 문제와 풀이의 품질 평가
후처리 — 학습용 대화 형식으로 변환
사전 요구사항
Python 3.10+
NVIDIA GPU가 장착된 환경
Super: 4x H100 (BF16), 2x H100 (FP8), 또는 1x B200 (NVFP4)
Nano: 1x H100/A100 (BF16), 1x H100 80GB (FP8), 또는 1x B200 (NVFP4)
Python과 Jupyter 노트북 기본 지식
Data Designer SDK 핵심 개념

NeMo Data Designer는 LLM을 활용하여 고품질 합성 데이터를 체계적으로 생성하는 NVIDIA의 SDK입니다.

주요 컴포넌트
컴포넌트	설명
SamplerColumn	LLM 호출 없이 결정론적으로 값을 샘플링 (카테고리, 분포 등)
LLMTextColumn	Jinja2 템플릿 기반 프롬프트로 LLM 텍스트 생성
LLMStructuredColumn	Pydantic 스키마에 맞는 구조화된 LLM 출력 생성
LLMJudgeColumn	LLM-as-a-Judge로 생성된 데이터 품질 평가 및 점수 부여
ExpressionColumn	Jinja2 템플릿으로 기존 컬럼을 변환 (LLM 호출 없음)
ValidationColumn	Python 함수로 생성된 데이터 검증
파이프라인 흐름
SamplerColumn (결정론적 분포) → LLMTextColumn (텍스트 생성) → LLMStructuredColumn (메타데이터 추출)
                                                            → LLMJudgeColumn (품질 평가)
                                                            → ExpressionColumn (포맷 변환)

컬럼 간 의존성은 Jinja2 템플릿의 {{ column_name }}으로 정의되며, Data Designer가 자동으로 실행 순서를 결정합니다.

참고 문서
리소스	링크
Nemotron 3 Super
Super vLLM Cookbook	GitHub
Super 모델 카드 (BF16)	HuggingFace
Super 모델 카드 (FP8)	HuggingFace
Super 모델 카드 (NVFP4)	HuggingFace
Nemotron 3 Nano
Nano vLLM Cookbook	GitHub
Nano 모델 카드 (BF16)	HuggingFace
Nano 모델 카드 (FP8)	HuggingFace
Nano 모델 카드 (NVFP4)	HuggingFace
일반
vLLM 문서	docs.vllm.ai
NeMo Data Designer 문서	nvidia-nemo.github.io
NeMo Data Designer GitHub	GitHub
NeMo Curator 문서	docs.nvidia.com
NVIDIA Build API	build.nvidia.com
Step 0: 환경 설정 & vLLM 배포
0.1 가상환경 생성 & 패키지 설치
python
Copy
# Step 0.1: Create virtual environment and register Jupyter kernel
import os

VENV_NAME = "track3"
VENV_PATH = os.path.join(os.getcwd(), VENV_NAME)

# Create virtual environment
!python3 -m venv {VENV_PATH}

# Install packages in the venv
!{VENV_PATH}/bin/pip install -q ipykernel data-designer openai pandas pyarrow requests

# Register as Jupyter kernel
!{VENV_PATH}/bin/python -m ipykernel install --user --name={VENV_NAME} --display-name="Python (track3)"

print(f"\n Virtual environment '{VENV_NAME}' created at: {VENV_PATH}")
print(f" Jupyter kernel '{VENV_NAME}' registered.")
print(f"\n Please select the 'Python (track3)' kernel from the Jupyter kernel menu before continuing.")
0.2 vLLM을 통한 Nemotron 3 배포

사용 가능한 GPU 리소스에 따라 모델을 선택하세요. 두 모델 모두 동일한 OpenAI 호환 API를 제공하므로, 나머지 파이프라인은 동일하게 작동합니다.

Nemotron 3 Super (120B A12B)
변형	모델 ID	필요 GPU
BF16	nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-BF16	4x H100 80GB
FP8	nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8	2x H100 80GB
NVFP4	nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4	1x B200
Nemotron 3 Nano (30B A3B)
변형	모델 ID	필요 GPU
BF16	nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16	1x H100/A100 80GB
FP8	nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-FP8	1x H100 80GB
NVFP4	nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4	1x B200

참고: vLLM 서버는 GPU 서버의 별도 터미널에서 실행하세요. 다음 셀을 진행하기 전에 서버가 실행 중이어야 합니다. 두 모델 모두 --served-model-name nemotron을 사용하므로, 클라이언트 코드는 모델 선택에 관계없이 동일합니다.

Step 0.2a: vLLM 패키지 설치
bash
Copy
%%script bash
# Step 0.2a: track3 활성화 후 vLLM 패키지 설치 (GPU·네트워크 필요)
set -euo pipefail
ACTIVATE="$(pwd)/track3/bin/activate"
if [ ! -f "$ACTIVATE" ]; then
  echo "먼저 track3 가상환경을 만드세요: $ACTIVATE" >&2
  exit 1
fi
# shellcheck disable=SC1091
source "$ACTIVATE"
pip install -U vllm==0.17.1 torch==2.10.0 flashinfer-python==0.6.4 \
  flashinfer-cubin==0.6.4 'nvidia-cutlass-dsl>=4.4.0.dev1' \
  --extra-index-url https://download.pytorch.org/whl/cu128

터미널에서 직접 설치할 때 예시 (track3은 Python venv이므로 conda activate가 아니라 bin/activate 사용):

bash
Copy
source "$(pwd)/track3/bin/activate"
pip install -U vllm==0.17.1 torch==2.10.0 flashinfer-python==0.6.4 \
  flashinfer-cubin==0.6.4 'nvidia-cutlass-dsl>=4.4.0.dev1' \
  --extra-index-url https://download.pytorch.org/whl/cu128
Step 0.2b: vLLM 실행 스크립트 생성

다음 셀이 scripts/launch_nemotron_super.sh와 scripts/launch_nemotron_nano.sh를 자동 생성합니다.

bash
Copy
%%script bash
# Step 0.2b: vLLM 실행 스크립트 생성 (`cat > … << 'DELIM'` = 터미널과 동일)
set -euo pipefail
mkdir -p scripts

cat > scripts/launch_nemotron_super.sh <<'END_OF_LAUNCH_SUPER_SH'
#!/usr/bin/env bash
# Nemotron 3 Super — vLLM 서버 (별도 터미널에서 실행)
# 사용법: ./launch_nemotron_super.sh {bf16|fp8|nvfp4}
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT"

usage() {
  cat <<'EOF'
Usage: launch_nemotron_super.sh {bf16|fp8|nvfp4}

  bf16   Option A — 4x H100 80GB (BF16)
  fp8    Option B — 2x H100 80GB (FP8)
  nvfp4  Option C — 1x B200 (NVFP4)

Reasoning parser와 작업 디렉터리는 이 스크립트가 있는 폴더(scripts/)에 둡니다.
EOF
  exit "${1:-0}"
}

case "${1:-}" in
  bf16|BF16|a|A)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-BF16/resolve/main/super_v3_reasoning_parser.py"
    exec vllm serve nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-BF16 \
      --async-scheduling \
      --dtype auto \
      --kv-cache-dtype fp8 \
      --tensor-parallel-size 4 \
      --pipeline-parallel-size 1 \
      --data-parallel-size 1 \
      --swap-space 0 \
      --trust-remote-code \
      --gpu-memory-utilization 0.9 \
      --enable-chunked-prefill \
      --max-num-seqs 512 \
      --served-model-name nemotron \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./super_v3_reasoning_parser.py" \
      --reasoning-parser super_v3
    ;;
  fp8|FP8|b|B)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8/resolve/main/super_v3_reasoning_parser.py"
    exec vllm serve nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8 \
      --async-scheduling \
      --dtype auto \
      --kv-cache-dtype fp8 \
      --tensor-parallel-size 2 \
      --attention-backend TRITON_ATTN \
      --gpu-memory-utilization 0.9 \
      --enable-chunked-prefill \
      --max-num-seqs 512 \
      --served-model-name nemotron \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./super_v3_reasoning_parser.py" \
      --reasoning-parser super_v3
    ;;
  nvfp4|NVFP4|c|C)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4/resolve/main/super_v3_reasoning_parser.py"
    exec vllm serve nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4 \
      --async-scheduling \
      --dtype auto \
      --kv-cache-dtype fp8 \
      --tensor-parallel-size 1 \
      --attention-backend TRITON_ATTN \
      --gpu-memory-utilization 0.9 \
      --enable-chunked-prefill \
      --max-num-seqs 512 \
      --served-model-name nemotron \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./super_v3_reasoning_parser.py" \
      --reasoning-parser super_v3
    ;;
  -h|--help|help) usage 0 ;;
  *)
    echo "Unknown option: ${1:-}" >&2
    usage 1
    ;;
esac
END_OF_LAUNCH_SUPER_SH
chmod +x scripts/launch_nemotron_super.sh

cat > scripts/launch_nemotron_nano.sh <<'END_OF_LAUNCH_NANO_SH'
#!/usr/bin/env bash
# Nemotron 3 Nano — vLLM 서버 (별도 터미널에서 실행)
# 사용법: ./launch_nemotron_nano.sh {bf16|fp8|nvfp4}
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT"

usage() {
  cat <<'EOF'
Usage: launch_nemotron_nano.sh {bf16|fp8|nvfp4}

  bf16   Option A — 1x H100/A100 80GB (BF16)
  fp8    Option B — 1x H100 80GB (FP8)
  nvfp4  Option C — 1x B200 (~20GB VRAM, NVFP4)

Reasoning parser와 작업 디렉터리는 이 스크립트가 있는 폴더(scripts/)에 둡니다.
준비되면 로그에 'Uvicorn running on http://0.0.0.0:5000' 가 보입니다.
EOF
  exit "${1:-0}"
}

case "${1:-}" in
  bf16|BF16|a|A)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16/resolve/main/nano_v3_reasoning_parser.py"
    exec python3 -m vllm.entrypoints.openai.api_server \
      --model nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16 \
      --dtype auto \
      --trust-remote-code \
      --served-model-name nemotron \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./nano_v3_reasoning_parser.py" \
      --reasoning-parser nano_v3
    ;;
  fp8|FP8|b|B)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-FP8/resolve/main/nano_v3_reasoning_parser.py"
    exec python3 -m vllm.entrypoints.openai.api_server \
      --model nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-FP8 \
      --dtype auto \
      --trust-remote-code \
      --served-model-name nemotron \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./nano_v3_reasoning_parser.py" \
      --reasoning-parser nano_v3
    ;;
  nvfp4|NVFP4|c|C)
    wget -nc "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4/resolve/main/nano_v3_reasoning_parser.py"
    exec env \
      VLLM_USE_FLASHINFER_MOE_FP4=1 \
      VLLM_FLASHINFER_MOE_BACKEND=throughput \
      vllm serve nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4 \
      --served-model-name nemotron \
      --max-num-seqs 8 \
      --tensor-parallel-size 1 \
      --max-model-len 262144 \
      --kv-cache-dtype fp8 \
      --trust-remote-code \
      --host 0.0.0.0 \
      --port 5000 \
      --enable-auto-tool-choice \
      --tool-call-parser qwen3_coder \
      --reasoning-parser-plugin "./nano_v3_reasoning_parser.py" \
      --reasoning-parser nano_v3
    ;;
  -h|--help|help) usage 0 ;;
  *)
    echo "Unknown option: ${1:-}" >&2
    usage 1
    ;;
esac
END_OF_LAUNCH_NANO_SH
chmod +x scripts/launch_nemotron_nano.sh

echo "생성됨: scripts/launch_nemotron_super.sh scripts/launch_nemotron_nano.sh"
Nemotron 3 Super — 터미널에서 실행

선행: Step 0.2b로 scripts/launch_nemotron_super.sh가 있어야 합니다.

bash
Copy
bash scripts/launch_nemotron_super.sh bf16    # Option A: BF16 (4x H100 80GB)
bash scripts/launch_nemotron_super.sh fp8     # Option B: FP8 (2x H100 80GB)
bash scripts/launch_nemotron_super.sh nvfp4   # Option C: NVFP4 (1x B200)

노트북에서 !bash scripts/launch_nemotron_super.sh bf16은 가능하지만 셀이 vLLM 종료까지 멈춥니다.

Nemotron 3 Nano — 터미널에서 실행

선행: Step 0.2b로 scripts/launch_nemotron_nano.sh가 있어야 합니다.

bash
Copy
bash scripts/launch_nemotron_nano.sh bf16    # Option A: BF16 (1x H100/A100 80GB)
bash scripts/launch_nemotron_nano.sh fp8     # Option B: FP8 (1x H100 80GB)
bash scripts/launch_nemotron_nano.sh nvfp4   # Option C: NVFP4 (1x B200)

준비되면 로그에 Uvicorn running on http://0.0.0.0:5000 이 보입니다.

0.3 vLLM 서버 연결 확인

데이터 생성 파이프라인을 진행하기 전에 vLLM 서버가 정상적으로 실행 중인지 확인합니다.

python
Copy
# Verify vLLM — try common ports
import requests

CANDIDATE_BASES = [
    "http://localhost:5000/v1",  # build_and_run.sh
    "http://localhost:8000/v1",  # vLLM default
]

VLLM_BASE_URL = None
response = None
last_err = None
for base in CANDIDATE_BASES:
    try:
        r = requests.get(f"{base}/models", timeout=10)
        r.raise_for_status()
        VLLM_BASE_URL = base
        response = r
        break
    except Exception as e:
        last_err = e

if VLLM_BASE_URL is None:
    print("Cannot connect on :5000 or :8000.")
    print(f"Last error: {last_err}")
else:
    models = response.json()
    ids = [m["id"] for m in models.get("data", [])]
    print(f"vLLM is running at {VLLM_BASE_URL}")
    print(f"Available models: {ids}")
    if "nemotron" in ids:
        VLLM_MODEL_NAME = "nemotron"
    elif ids:
        VLLM_MODEL_NAME = ids[0]
    else:
        VLLM_MODEL_NAME = "nemotron"
python
Copy
import os

# vLLM Server Configuration
_g = globals()
if _g.get("VLLM_BASE_URL") is None:
    VLLM_BASE_URL = "http://localhost:5000/v1"
if _g.get("VLLM_MODEL_NAME") is None:
    VLLM_MODEL_NAME = "nemotron"

# Set environment variables for Data Designer
os.environ["NVIDIA_BASE_URL"] = VLLM_BASE_URL
os.environ["NVIDIA_API_KEY"] = "not-used"  # Required by Data Designer but not validated by vLLM

print(f"Configured to use vLLM server at: {VLLM_BASE_URL}")
print(f"Model name: {VLLM_MODEL_NAME}")
python
Copy
from data_designer.interface import DataDesigner
from data_designer.config import (
    CategorySamplerParams,
    ChatCompletionInferenceParams,
    DataDesignerConfigBuilder,
    ExpressionColumnConfig,
    LLMJudgeColumnConfig,
    LLMStructuredColumnConfig,
    LLMTextColumnConfig,
    ModelConfig,
    ModelProvider,
    SamplerColumnConfig,
    SamplerType,
    Score,
)
from pydantic import BaseModel, Field
from typing import List

print("모든 import 완료!")
Step 1: 데이터 스키마 정의

합성 데이터셋은 다음 컬럼으로 구성됩니다:

컬럼	타입	설명
grade	Sampler (Category)	대상 학년 (초3–중3)
topic	Sampler (Category)	수학 주제 영역
difficulty	Sampler (Category)	쉬움 / 보통 / 어려움
problem	LLM Text	한국어 수학 문제
solution	LLM Text	한국어 단계별 CoT 풀이
answer_metadata	LLM Structured	추출된 정답 + 사용 개념
quality_score	LLM Judge	문제/풀이 품질 평가 점수
chat_formatted	Expression	학습용 대화 형식

핵심 원리: 샘플러 컬럼이 먼저 생성되고 (빠르고 결정론적), LLM 컬럼이 Jinja2 템플릿으로 이를 참조하여 맥락에 맞는 콘텐츠를 생성합니다.

1.1 LLM 모델 설정

로컬 vLLM 엔드포인트에서 서빙되는 Nemotron 3 (Super 또는 Nano)를 생성 모델로 사용합니다. Data Designer는 서로 다른 별칭(alias)과 추론 파라미터를 가진 여러 모델 설정을 정의할 수 있습니다.

python
Copy
# --- 모델 설정 ---
# 로컬 vLLM 엔드포인트의 Nemotron 3 사용

# vLLM 서버를 가리키는 커스텀 프로바이더 정의
vllm_provider = ModelProvider(
    name="vllm-local",
    endpoint=VLLM_BASE_URL,
    provider_type="openai",  # vLLM은 OpenAI 호환 API 제공
    api_key="not-used",
)

# 다양한 수학 문제 생성을 위한 창의적 모델
problem_model = ModelConfig(
    alias="problem-generator",
    model=VLLM_MODEL_NAME,
    provider="vllm-local",
    inference_parameters=ChatCompletionInferenceParams(
        temperature=0.9,
        top_p=0.95,
        max_tokens=4096,
    ),
)

# 단계별 풀이 생성을 위한 정밀 모델
solution_model = ModelConfig(
    alias="solution-generator",
    model=VLLM_MODEL_NAME,
    provider="vllm-local",
    inference_parameters=ChatCompletionInferenceParams(
        temperature=0.3,  # 정확한 추론을 위해 낮은 temperature
        top_p=0.9,
        max_tokens=2048,
    ),
)

# 구조화된 메타데이터 추출 모델
structured_model = ModelConfig(
    alias="extractor",
    model=VLLM_MODEL_NAME,
    provider="vllm-local",
    inference_parameters=ChatCompletionInferenceParams(
        temperature=0.1,
        max_tokens=512,
    ),
)

# 품질 평가용 LLM Judge 모델
judge_model = ModelConfig(
    alias="quality-judge",
    model=VLLM_MODEL_NAME,
    provider="vllm-local",
    inference_parameters=ChatCompletionInferenceParams(
        temperature=0.1,  # 일관된 평가를 위해 낮은 temperature
        max_tokens=1024,
    ),
)

print(f"vLLM 프로바이더({VLLM_BASE_URL})를 사용하는 모델 설정 완료")
print(f"  - problem-generator (temp=0.9)")
print(f"  - solution-generator (temp=0.3)")
print(f"  - extractor (temp=0.1)")
print(f"  - quality-judge (temp=0.1)")
1.2 샘플러 컬럼 정의

샘플러 컬럼은 결정론적입니다 — LLM을 호출하지 않습니다. 학년, 주제, 난이도 수준 전반에 걸쳐 균형 잡힌 데이터셋 분포를 제어합니다.

팁: weights를 조정하여 각 카테고리의 비율을 제어할 수 있습니다. 예를 들어, "쉬움"이나 "어려움"보다 "보통" 난이도의 문제를 더 많이 생성할 수 있습니다.

python
Copy
# --- Sampler Columns ---

# Grade level: Korean elementary (초등) and middle school (중등)
grade_column = SamplerColumnConfig(
    name="grade",
    sampler_type=SamplerType.CATEGORY,
    params=CategorySamplerParams(
        values=[
            "초등학교 3학년",  # Elementary 3rd
            "초등학교 4학년",  # Elementary 4th
            "초등학교 5학년",  # Elementary 5th
            "초등학교 6학년",  # Elementary 6th
            "중학교 1학년",    # Middle 1st
            "중학교 2학년",    # Middle 2nd
            "중학교 3학년",    # Middle 3rd
        ],
        weights=[0.10, 0.12, 0.15, 0.15, 0.18, 0.15, 0.15],
    ),
)

# Math topics aligned with Korean curriculum
topic_column = SamplerColumnConfig(
    name="topic",
    sampler_type=SamplerType.CATEGORY,
    params=CategorySamplerParams(
        values=[
            "사칙연산 (Arithmetic)",
            "분수와 소수 (Fractions & Decimals)",
            "도형과 측정 (Geometry & Measurement)",
            "비와 비율 (Ratios & Proportions)",
            "방정식 (Equations)",
            "함수와 그래프 (Functions & Graphs)",
            "확률과 통계 (Probability & Statistics)",
            "규칙과 패턴 (Patterns & Sequences)",
        ],
        weights=[0.15, 0.15, 0.12, 0.12, 0.13, 0.10, 0.10, 0.13],
    ),
)

# Difficulty distribution — more medium problems for balanced training
difficulty_column = SamplerColumnConfig(
    name="difficulty",
    sampler_type=SamplerType.CATEGORY,
    params=CategorySamplerParams(
        values=["쉬움 (Easy)", "보통 (Medium)", "어려움 (Hard)"],
        weights=[0.25, 0.50, 0.25],
    ),
)

print("Sampler columns defined: grade, topic, difficulty")
1.3 LLM 텍스트 컬럼 정의 — 문제 & 풀이 생성

핵심 단계입니다. Jinja2 템플릿으로 샘플러 컬럼을 참조하여, LLM이 샘플링된 학년, 주제, 난이도에 맞는 문제를 생성합니다.

solution 컬럼이 {{ problem }}을 참조하는 것을 확인하세요 — Data Designer가 자동으로 의존성 순서를 처리합니다.

python
Copy
# --- LLM Text Columns ---

PROBLEM_PROMPT = """\
You are an expert Korean math education specialist.

Create a math problem in Korean for the following specifications:
- Grade level: {{ grade }}
- Topic: {{ topic }}
- Difficulty: {{ difficulty }}

Requirements:
- The problem MUST be written entirely in Korean.
- Use a real-world context that Korean students can relate to \
(e.g., school life, shopping, cooking, sports, travel in Korea).
- The problem should be solvable with knowledge appropriate for the given grade level.
- Include specific numbers and clearly state what needs to be found.
- Do NOT include the solution or answer — only the problem statement.

Output ONLY the problem text in Korean, nothing else.

Format:
## 문제
[problem text]
"""

problem_column = LLMTextColumnConfig(
    name="problem",
    prompt=PROBLEM_PROMPT,
    model_alias="problem-generator",
)

SOLUTION_PROMPT = """\
You are an expert Korean math tutor who explains solutions step by step.

Solve the following math problem with a detailed Chain-of-Thought explanation in Korean.

Problem ({{ grade }}, {{ topic }}, {{ difficulty }}):
{{ problem }}

Requirements:
- Write the ENTIRE solution in Korean.
- Break down the solution into clear, numbered steps (풀이 과정).
- Explain the reasoning behind each step so a {{ grade }} student can understand.
- Use mathematical notation where appropriate (e.g., +, -, ×, ÷, =).
- End with the final answer clearly marked as: **정답: [answer]**

Format:
## 풀이 과정

1단계: [first step explanation]
2단계: [second step explanation]
...

**정답: [final answer]**
"""

solution_column = LLMTextColumnConfig(
    name="solution",
    prompt=SOLUTION_PROMPT,
    model_alias="solution-generator",
)

print("LLM text columns defined: problem, solution")
1.4 구조화 출력 컬럼 정의 — 정답 메타데이터

LLMStructuredColumnConfig과 Pydantic 모델을 사용하여 생성된 풀이에서 구조화된 메타데이터를 추출합니다. 이를 통해 출력이 엄격한 스키마를 따르도록 보장하며, 후속 필터링과 평가에 유용합니다.

python
Copy
# --- Structured Output Column ---

class MathAnswerMetadata(BaseModel):
    """Structured metadata extracted from a math problem and its solution."""
    final_answer: str = Field(description="The final numerical or expression answer")
    num_steps: int = Field(description="Number of reasoning steps in the solution")
    concepts_used: List[str] = Field(description="List of math concepts used (in Korean)")
    requires_diagram: bool = Field(description="Whether the problem would benefit from a diagram")

METADATA_PROMPT = """\
Analyze the following Korean math problem and its solution.
Extract the requested metadata accurately.

Problem: {{ problem }}
Solution: {{ solution }}

Extract:
1. The final answer (just the value/expression)
2. The number of reasoning steps
3. List of mathematical concepts used (in Korean, e.g., ["덧셈", "곱셈", "비율"])
4. Whether a diagram would help understand the problem
"""

metadata_column = LLMStructuredColumnConfig(
    name="answer_metadata",
    prompt=METADATA_PROMPT,
    output_format=MathAnswerMetadata,
    model_alias="extractor",
)

print("Structured column defined: answer_metadata")
print(f"Schema: {MathAnswerMetadata.model_json_schema()}")
1.5 LLM Judge 컬럼 정의 — 품질 평가

LLMJudgeColumnConfig을 사용하여 생성된 문제와 풀이의 품질을 자동으로 평가합니다. Score 객체로 평가 기준(루브릭)을 정의하면, LLM이 각 기준에 따라 점수를 부여합니다. 이를 통해 저품질 샘플을 필터링할 수 있습니다.

python
Copy
# --- LLM Judge column: quality evaluation ---

# Criterion 1: Problem quality
problem_quality_score = Score(
    name="problem_quality",
    description="Overall quality of the math problem: clarity, fit for the stated grade, and use of realistic context.",
    options={
        1: "Unclear or off-grade; not solvable as stated or ambiguous.",
        2: "Mostly understandable but context feels unnatural or difficulty is inappropriate.",
        3: "Clear, grade-appropriate, and uses reasonable real-life context.",
        4: "Strong: clear, pedagogical, and context fits Korean students' everyday situations.",
        5: "Excellent: creative, high educational value, ideal difficulty and context.",
    },
)

# Criterion 2: Solution correctness
solution_correctness_score = Score(
    name="solution_correctness",
    description="Mathematical correctness and logical completeness: each step and the final answer.",
    options={
        1: "Serious math errors; wrong calculations or invalid reasoning.",
        2: "Right direction but notable calculation mistakes or gaps in logic.",
        3: "Mathematically sound and logically valid; final answer is correct.",
        4: "Accurate with clear step-by-step explanation; easy for a student to follow.",
        5: "Flawless: accurate, excellent stepwise explanation, strong pedagogical insight.",
    },
)

# Criterion 3: CoT explanation quality
cot_quality_score = Score(
    name="cot_quality",
    description="Quality of the chain-of-thought: step structure, justification of each step, and readability for the target grade.",
    options={
        1: "Little explanation or answer-only; not real CoT.",
        2: "Some steps but explanations are thin or not at the right level.",
        3: "Adequate step-by-step explanation; a student can follow along.",
        4: "Detailed and educational; rationale for each step is explicit.",
        5: "Outstanding: intuitive, highly educational, and supports mathematical thinking.",
    },
)

JUDGE_PROMPT = """\
You are an expert evaluator for Korean K–12 mathematics.

Evaluate the following **Korean** word problem and **Korean** step-by-step solution \
using the scoring rubrics (configured separately).

**Language policy**
- The problem and solution text below are in Korean; judge them on that content.
- For any rationale, brief comments, or free-form reasoning you output as part of this \
judgment, write in **Korean** (한국어).

**Grade:** {{ grade }}
**Topic:** {{ topic }}
**Difficulty:** {{ difficulty }}

**Problem (Korean):**
{{ problem }}

**Solution (Korean):**
{{ solution }}

Assign scores according to the rubric.
"""

# LLM Judge column
quality_judge_column = LLMJudgeColumnConfig(
    name="quality_score",
    prompt=JUDGE_PROMPT,
    model_alias="quality-judge",
    scores=[problem_quality_score, solution_correctness_score, cot_quality_score],
)

print("LLM Judge column defined: quality_score")
print("Rubrics: problem_quality (1-5), solution_correctness (1-5), cot_quality (1-5)")
1.6 표현식 컬럼 정의 — 학습용 대화 형식

표현식 컬럼은 LLM 호출 없이 Jinja2 템플릿으로 기존 컬럼을 변환합니다. 여기서는 각 행을 SFT(Supervised Fine-Tuning)에 사용할 대화 메시지 형식으로 포맷합니다.

python
Copy
# --- Expression Column: Chat Format ---

SYSTEM_MESSAGE = "당신은 한국 수학 교육 전문가입니다. 학생의 수준에 맞춰 친절하고 단계적으로 수학 문제를 풀어주세요."

chat_column = ExpressionColumnConfig(
    name="chat_formatted",
    expr=(
        '[{"role": "system", "content": "' + SYSTEM_MESSAGE + '"},'
        ' {"role": "user", "content": "{{ problem }}"},'
        ' {"role": "assistant", "content": "{{ solution }}"}]'
    ),
)

print("Expression column defined: chat_formatted")
Step 2: 파이프라인 구성 & 미리보기

모든 컬럼을 DataDesignerConfigBuilder에 조립하고, 스케일업 전에 소량의 샘플로 미리보기를 실행하여 파이프라인을 검증합니다.

python
Copy
# --- 파이프라인 구성 ---

config_builder = DataDesignerConfigBuilder(
    model_configs=[problem_model, solution_model, structured_model, judge_model],
)

# 의존성 순서대로 컬럼 추가
# (Data Designer가 자동으로 순서를 결정하지만, 명시적 순서가 가독성에 도움)
config_builder.add_column(grade_column)         # Sampler — 의존성 없음
config_builder.add_column(topic_column)         # Sampler — 의존성 없음
config_builder.add_column(difficulty_column)    # Sampler — 의존성 없음
config_builder.add_column(problem_column)       # LLM — grade, topic, difficulty에 의존
config_builder.add_column(solution_column)      # LLM — problem, grade, topic, difficulty에 의존
config_builder.add_column(metadata_column)      # LLM Structured — problem, solution에 의존
config_builder.add_column(quality_judge_column) # LLM Judge — problem, solution, grade 등에 의존
config_builder.add_column(chat_column)          # Expression — problem, solution에 의존

print("파이프라인 컬럼 등록 완료.")
미리보기 실행
python
Copy
# --- 미리보기: 소량 샘플 생성 ---
data_designer = DataDesigner(model_providers=[vllm_provider])

data_designer.validate(config_builder)
print("설정 검증 완료.")

preview = data_designer.preview(config_builder=config_builder, num_records=5)
print(f"{len(preview.dataset)}개의 미리보기 레코드 생성 완료!")
python
Copy
# --- 샘플 레코드 확인 ---
preview.display_sample_record(index=0)
python
Copy
# --- 전체 미리보기 DataFrame 확인 ---
df_preview = preview.dataset
df_preview[["grade", "topic", "difficulty", "problem", "quality_score", "answer_metadata"]].head()
전체 예제 확인

생성된 문제-풀이 쌍을 자세히 살펴보며 품질을 검증합니다.

python
Copy
# --- 전체 예제 상세 출력 ---
row = df_preview.iloc[0]

print("=" * 60)
print(f"학년:   {row['grade']}")
print(f"주제:   {row['topic']}")
print(f"난이도: {row['difficulty']}")
print("=" * 60)
print(f"\n문제:\n{row['problem']}")
print(f"\n풀이:\n{row['solution']}")
print(f"\n메타데이터:\n{row['answer_metadata']}")
print(f"\n품질 평가:\n{row['quality_score']}")
print("=" * 60)
Step 3: 대규모 생성

미리보기 품질에 만족하면, 전체 데이터셋을 생성합니다. Data Designer가 배치 처리, 병렬화, 재시도를 자동으로 처리합니다.

python
Copy
# --- 대규모 배치 생성 ---
# 필요와 API 예산에 따라 num_records를 조정하세요
# 테스트용으로는 작게 셋팅, 실제 데이터셋은 1000+ 권장

NUM_RECORDS = 30  # 프로덕션 데이터셋에는 500-1000+로 변경

creation_results = data_designer.create(
    config_builder=config_builder,
    num_records=NUM_RECORDS,
    dataset_name="korean-math-cot",
)
print("\n생성 완료!")
python
Copy
# --- 생성된 데이터셋 로드 및 확인 ---
df = creation_results.load_dataset()
df.head()
python
Copy
print(f"총 생성 레코드 수: {len(df)}")
print(f"\n컬럼: {list(df.columns)}")
print(f"\n학년 분포:\n{df['grade'].value_counts()}")
print(f"\n주제 분포:\n{df['topic'].value_counts()}")
print(f"\n난이도 분포:\n{df['difficulty'].value_counts()}")
Step 4: 품질 분석 & LLM Judge 기반 필터링

생성된 데이터셋의 품질을 분석하고, LLM Judge 점수와 기본 휴리스틱을 결합하여 필터링합니다.

python
Copy
# --- 품질 분석: 기본 체크 + LLM Judge 점수 파싱 ---
import json
import pandas as pd

def parse_metadata(meta_str):
    """answer_metadata 컬럼 파싱 (문자열 또는 딕셔너리)."""
    if isinstance(meta_str, dict):
        return meta_str
    try:
        return json.loads(meta_str)
    except (json.JSONDecodeError, TypeError):
        return None

def parse_judge_scores(val):
    """quality_score 컬럼을 dict로 정규화 (JSON 문자열 또는 dict)."""
    if val is None or (isinstance(val, float) and pd.isna(val)):
        return None
    if isinstance(val, dict):
        return val
    if isinstance(val, str):
        try:
            return json.loads(val)
        except (json.JSONDecodeError, TypeError):
            return None
    return None

def extract_criterion_score(parsed: dict | None, name: str):
    """한 기준의 점수만 추출."""
    if not isinstance(parsed, dict):
        return None
    v = parsed.get(name)
    if v is None:
        return None
    if isinstance(v, dict):
        return v.get("score")
    return v

df["parsed_meta"] = df["answer_metadata"].apply(parse_metadata)
df["parsed_scores"] = df["quality_score"].apply(parse_judge_scores)

for key, col in [
    ("problem_quality", "score_problem_quality"),
    ("solution_correctness", "score_solution_correctness"),
    ("cot_quality", "score_cot_quality"),
]:
    df[col] = df["parsed_scores"].apply(lambda x, k=key: extract_criterion_score(x, k))

for col in ["score_problem_quality", "score_solution_correctness", "score_cot_quality"]:
    df[col] = pd.to_numeric(df[col], errors="coerce")

score_cols = ["score_problem_quality", "score_solution_correctness", "score_cot_quality"]
df["avg_quality_score"] = df[score_cols].mean(axis=1)

# 기본 형식 체크 (한국어 키워드)
df["solution_length"] = df["solution"].str.len()
df["problem_length"] = df["problem"].str.len()
df["has_steps"] = df["solution"].str.contains("단계", na=False)
df["has_answer"] = df["solution"].str.contains("정답", na=False)

print("=== 품질 지표 ===")
print(f"  - 평균 풀이 길이: {df['solution_length'].mean():.0f}자")
print(f"  - 평균 문제 길이: {df['problem_length'].mean():.0f}자")
print(f"  - 단계 표시 포함: {df['has_steps'].sum()}/{len(df)}")
print(f"  - 정답 표시 포함: {df['has_answer'].sum()}/{len(df)}")
print()
print("=== LLM Judge 평가 점수 (1-5점) ===")
python
Copy
# --- LLM Judge 점수 기반 고품질 필터링 ---
# Judge 점수 임계값: 각 기준 3점 이상 (보통 이상) & 평균 3.5점 이상

JUDGE_THRESHOLD_MIN = 3    # 각 개별 기준의 최소 점수
JUDGE_THRESHOLD_AVG = 3.5  # 종합 평균 최소 점수

df_filtered = df[
    # 기본 형식 필터
    (df["has_steps"]) &                              # 단계별 형식 필수
    (df["has_answer"]) &                             # 정답 표시 필수
    (df["solution_length"] > 200) &                  # 풀이가 충분히 길어야 함
    (df["parsed_meta"].notna()) &                    # 메타데이터 파싱 가능해야 함
    # LLM Judge 점수 필터
    (df["score_problem_quality"] >= JUDGE_THRESHOLD_MIN) &
    (df["score_solution_correctness"] >= JUDGE_THRESHOLD_MIN) &
    (df["score_cot_quality"] >= JUDGE_THRESHOLD_MIN) &
    (df["avg_quality_score"] >= JUDGE_THRESHOLD_AVG)
].copy()

print(f"필터링 결과: {len(df_filtered)}/{len(df)}개 ({len(df_filtered)/len(df)*100:.1f}% 유지)")
print(f"\n필터링 후 평균 품질 점수: {df_filtered['avg_quality_score'].mean():.2f}")
Step 5: 데이터셋 내보내기

모델 학습에 바로 사용할 수 있는 형식으로 최종 데이터셋을 내보냅니다.

python
Copy
# --- 내보내기: JSONL 형식 (LLM 파인튜닝 표준) ---
import json
from pathlib import Path

output_dir = Path("output")
output_dir.mkdir(exist_ok=True)

# SFT용 대화 형식 데이터 내보내기
sft_records = []
for _, row in df_filtered.iterrows():
    record = {
        "messages": [
            {
                "role": "system",
                "content": "당신은 한국 수학 교육 전문가입니다. 학생의 수준에 맞춰 친절하고 단계적으로 수학 문제를 풀어주세요.",
            },
            {"role": "user", "content": row["problem"]},
            {"role": "assistant", "content": row["solution"]},
        ],
        "metadata": {
            "grade": row["grade"],
            "topic": row["topic"],
            "difficulty": row["difficulty"],
            "quality_scores": {
                "problem_quality": row.get("score_problem_quality"),
                "solution_correctness": row.get("score_solution_correctness"),
                "cot_quality": row.get("score_cot_quality"),
                "avg_score": row.get("avg_quality_score"),
            },
        },
    }
    sft_records.append(record)

sft_path = output_dir / "korean_math_cot_sft.jsonl"
with open(sft_path, "w", encoding="utf-8") as f:
    for record in sft_records:
        f.write(json.dumps(record, ensure_ascii=False) + "\n")

print(f"{len(sft_records)}개 레코드를 {sft_path}에 내보냈습니다")

# 분석용 전체 DataFrame을 parquet으로 저장
parquet_path = output_dir / "korean_math_cot_full.parquet"
df_filtered.to_parquet(parquet_path, index=False)
print(f"전체 데이터셋을 {parquet_path}에 저장했습니다")
Step 6 (선택사항): HuggingFace Hub에 업로드

커뮤니티에 데이터셋을 공유하거나 후속 학습에 활용합니다.

python
Copy
# --- (Optional) Push to HuggingFace Hub ---
# Uncomment after setting HF_TOKEN

# import os
# os.environ["HF_TOKEN"] = "hf_..."
#
# url = creation_results.push_to_hub(
#     repo_id="your-username/korean-math-cot-dataset",
#     private=False,
# )
# print(f"Dataset pushed to: {url}")
Step 7 (보너스): vLLM 서버 관리 & 직접 API 테스트

vLLM 배포 관리와 OpenAI 호환 API를 통한 Nemotron 3 모델 직접 테스트에 유용한 명령어입니다. Super와 Nano 모두 동일하게 작동합니다.

python
Copy
# --- Direct vLLM API Test ---
from openai import OpenAI

client = OpenAI(base_url=VLLM_BASE_URL, api_key="not-used")

# Quick test: simple math question with reasoning disabled for speed
response = client.chat.completions.create(
    model=VLLM_MODEL_NAME,
    messages=[
        {"role": "system", "content": "You are a helpful math tutor. Answer in Korean."},
        {"role": "user", "content": "3 + 5 x 2 = ? 풀이 과정을 알려주세요."},
    ],
    temperature=0.3,
    max_tokens=512,
    extra_body={"chat_template_kwargs": {"enable_thinking": False}},
)
print("vLLM Direct Test Response:")
print(response.choices[0].message.content)

# --- With reasoning (thinking) mode enabled ---
# response_think = client.chat.completions.create(
#     model=VLLM_MODEL_NAME,
#     messages=[...],
#     temperature=1,
#     max_tokens=1024,
# )
# print("Reasoning:", response_think.choices[0].message.reasoning_content)
# print("Answer:", response_think.choices[0].message.content)

# --- Streaming ---
# stream = client.chat.completions.create(
#     model=VLLM_MODEL_NAME,
#     messages=[...],
#     stream=True,
# )
# for chunk in stream:
#     if chunk.choices[0].delta.content:
#         print(chunk.choices[0].delta.content, end="", flush=True)
요약 & 다음 단계

이 튜토리얼에서는 vLLM으로 서빙한 Nemotron 3 (Super 또는 Nano)와 NeMo Data Designer를 사용하여 한국어 수학 Chain-of-Thought 데이터셋을 생성하는 완전한 합성 데이터 파이프라인을 구축했습니다.

단계	수행 내용	핵심 개념
0	가상환경 설정, vLLM으로 Nemotron 3 배포	로컬 모델 서빙
1	샘플러 + LLM 컬럼으로 스키마 정의	컬럼 타입 & 의존성
2	소량 샘플로 미리보기	빠른 반복 루프
3	전체 데이터셋 생성	배치 생성
4	LLM Judge 기반 품질 평가 & 필터링	LLM-as-a-Judge
5	학습용 형식으로 내보내기	JSONL / Parquet 형식
파이프라인 확장 아이디어
검증 컬럼 추가: ValidationColumnConfig으로 커스텀 Python 함수를 사용하여 수학 정답을 프로그래밍적으로 검증
커리큘럼 학습: 난이도와 num_steps로 정렬하여 커리큘럼 순서의 학습 세트 구성
NeMo Curator로 중복 제거: 의미적 중복 제거를 통해 유사 문제 제거
다국어 지원: 한국어 + 영어 이중 언어 문제-풀이 쌍 생성으로 파이프라인 확장
난이도 보정: 구조화된 메타데이터로 난이도 라벨이 실제 풀이 복잡도와 일치하는지 확인
모델 비교: Super와 Nano 모두로 데이터셋을 생성하고, 품질 대비 비용 최적점 탐색